import style1 from '../../styles/about/About.module.scss'
const AboutSection = ({ children, style }) => {
    return (
        <div className={style1.about_section__13654 + " ui container"} style={style}>
            {children}
        </div>
    )
}

export default AboutSection;